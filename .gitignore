node_modules
dist
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Loose HTML files dropped in the repo root (study material scratch files,
# not part of the app — real content lives under public/, codex/, labs/).
# Added 2026-08-02 after several got swept in by an unrelated `git add -A`.
/*.html
!/index.html
__pycache__/

# Page images rendered from the Computer Operator scans for the System Manager
# vision extraction pass (16 MB of 400dpi PNGs). Regenerate on demand with
# `python3 tools/system-manager-build/render_pages.py` — the source PDFs live in
# the mpsc-question-bank repo, so these are a derived artifact, not an input.
/tools/system-manager-build/pages/
